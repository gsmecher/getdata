name: Wheels

on:
  workflow_dispatch:
  pull_request:
    branches: [ master ]
  release:
    types: [ published ]

jobs:
  build_wheels:
    name: Build wheels for ${{ matrix.build }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # macos-15-intel for x86_64 (min macOS 15.0), macos-14 for ARM (min macOS 14.0)
          - os: macos-15-intel
            python: '3.9'
            build: 'cp39-macosx_x86_64'
            target: '15.0'
            prefix: /usr/local

          - os: macos-15-intel
            python: '3.10'
            build: 'cp310-macosx_x86_64'
            target: '15.0'
            prefix: /usr/local

          - os: macos-15-intel
            python: '3.11'
            build: 'cp311-macosx_x86_64'
            target: '15.0'
            prefix: /usr/local

          - os: macos-15-intel
            python: '3.12'
            build: 'cp312-macosx_x86_64'
            target: '15.0'
            prefix: /usr/local

          - os: macos-14
            python: '3.9'
            build: 'cp39-macosx_arm64'
            target: '14.0'
            prefix: /opt/homebrew

          - os: macos-14
            python: '3.10'
            build: 'cp310-macosx_arm64'
            target: '14.0'
            prefix: /opt/homebrew

          - os: macos-14
            python: '3.11'
            build: 'cp311-macosx_arm64'
            target: '14.0'
            prefix: /opt/homebrew

          - os: macos-14
            python: '3.12'
            build: 'cp312-macosx_arm64'
            target: '14.0'
            prefix: /opt/homebrew

          - os: ubuntu-latest
            python: '3.12'
            build: 'cp*-manylinux_x86_64'
            prefix: /usr

          - os: windows-latest
            python: '3.12'
            build: 'cp*-win_amd64'
            prefix: C:/vcpkg/installed/x64-windows

    steps:
      - name: Setup environment
        run: echo "PREFIX=${{ matrix.prefix }}" >> $GITHUB_ENV

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      # Used to host cibuildwheel
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Setup macOS environment
        if: runner.os == 'macOS'
        run: |
          brew install --quiet automake libtool bzip2 flac xz pcre libzzip
          echo "MACOSX_DEPLOYMENT_TARGET=${{ matrix.target }}" >> $GITHUB_ENV
          echo CPPFLAGS="`python3-config --includes`" >> $GITHUB_ENV

      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel==2.21.3

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse bindings/python
        env:
          CIBW_BUILD: ${{ matrix.build }}
          CIBW_SKIP: cp36* cp37* cp38*
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y bzip2-devel flac-devel xz-devel pcre-devel zziplib-devel &&
            .github/workflows/bootstrap.sh --disable-python &&
            make
          CIBW_BEFORE_ALL_MACOS: >
            .github/workflows/bootstrap.sh --disable-python &&
            make
          CIBW_BEFORE_ALL_WINDOWS: >
            C:\msys64\usr\bin\bash -lc "pacman -Sy --noconfirm autoconf automake libtool make mingw-w64-x86_64-gcc mingw-w64-x86_64-pcre mingw-w64-x86_64-pcre2 mingw-w64-x86_64-libltdl mingw-w64-x86_64-libsystre" &&
            C:\msys64\msys2_shell.cmd -mingw64 -defterm -no-start -here -c ".github/workflows/bootstrap-windows.sh --disable-python && make"
          CIBW_BEFORE_BUILD: >
            pip install "numpy>=2.0" &&
            .github/workflows/bootstrap.sh &&
            make -C bindings/python pyconstants.c
          CIBW_BEFORE_BUILD_WINDOWS: >
            pip install "numpy>=2.0" &&
            C:\msys64\msys2_shell.cmd -mingw64 -defterm -no-start -here -c "make -C bindings/python pyconstants.c"
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            LD_LIBRARY_PATH=$PWD/src/.libs auditwheel repair -w {dest_dir} {wheel}
          CIBW_REPAIR_WHEEL_COMMAND_MACOS: >
            DYLD_LIBRARY_PATH=$PWD/src/.libs delocate-wheel --require-archs {delocate_archs} -w {dest_dir} -v {wheel}
          CIBW_BUILD_VERBOSITY: 1
          CIBW_ENVIRONMENT_WINDOWS: >
            PATH="C:\\msys64\\mingw64\\bin;$PATH"
            CC=gcc
            CXX=g++
            MSYS2_PATH_TYPE=inherit
          CIBW_TEST_COMMAND: >
            cd {package}/test &&
            PYTHONPATH=`python -c 'import sysconfig; print(sysconfig.get_paths()["purelib"])'`:$PYTHONPATH make check

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cibw-wheel-${{ matrix.os }}-${{ matrix.python }}
          path: ./wheelhouse/pygetdata*.whl

  upload_pypi:
    needs: [build_wheels]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    if: github.event_name == 'release' && github.event.action == 'published'
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          # unpacks all CIBW artifacts into dist/
          pattern: cibw-*
          path: dist
          merge-multiple: true

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
